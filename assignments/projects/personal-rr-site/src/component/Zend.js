import React from "react";
import { connect } from 'react-redux'

    const Zend = (props) => {
        console.log(this.props.data.quoteText);
        return (
            <div className="fav">
            <h1>Love Yourself Always & In All Ways</h1>
            </div>
        )
    }
    export default Zend;